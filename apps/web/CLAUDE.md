# apps/web — `@twy/web`

Reserved for the Astro marketing site. No code yet — only the workspace stub. When this site goes live it will share the multi-domain `sst.aws.Router` with `@twy/dashboard`, with the marketing site mounted on the apex (`/`) and the dashboard on a subpath (or a separate domain — design pending).

> Read root `CLAUDE.md` first.
