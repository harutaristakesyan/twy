import { Typography } from "antd";

const { Title } = Typography;

export default function InternalBillingPage() {
  return (
    <div style={{ padding: 24 }}>
      <Title level={4}>Internal Billing</Title>
    </div>
  );
}
