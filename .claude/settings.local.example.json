{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "TEMPLATE — do NOT edit this file with personal values. Copy it to settings.local.json (gitignored) and edit that copy. settings.local.json wins over settings.json. Use it to: change default model for your machine, override ENV (e.g. set ENV=prod for read-only production audits), allow/deny extra commands, switch off the post-edit autoformat hook, etc.",

  "model": "claude-sonnet-4-6",

  "env": {
    "ENV": "dev",
    "AWS_REGION": "us-east-1",
    "AWS_PROFILE": "twy-dev"
  },

  "permissions": {
    "allow": ["Bash(open:*)"],
    "deny": []
  }
}
